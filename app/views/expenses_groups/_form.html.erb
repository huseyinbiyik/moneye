<%= form_with(model: expenses_group) do |form| %>
  <% if expenses_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expenses_group.errors.count, "error") %> prohibited this expenses_group from being saved:</h2>

      <ul>
        <% expenses_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :expense_id, style: "display: block" %>
    <%= form.text_field :expense_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
